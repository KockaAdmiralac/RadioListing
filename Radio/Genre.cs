namespace Radio
{
    public class Genre
    {
        public string name, id;
        public Genre(string name, string id)
        {
            this.name = name;
            this.id = id;
        }
    }
}
